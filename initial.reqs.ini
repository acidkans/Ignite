ETAP 0 — Decyzje startowe (jednorazowe)
aplikacja webowa klasy erp obsługująca podstawowe procesy w firmie
online first
laptop first
1.	struktura drzewa
area(administracja)->field(wnioski urlopowe, raporty urlopów)->detail(moje wnioski, moje dane)
area(projekty)->field(klient w domyśle "projekt")->order(zamówienie)->site(lokalizacja)->subtask(logistyka, instalacja,dokumentacja)
area(prywatne)-field(kazdy użytkownik może zamówić swoją dziedzinę, którą chce tutaj widzieć
👉 Zanim napiszesz linijkę kodu
•	Frontend: React
•	CSS:Tailwind
•	Backend: NestJS
•	DB: PostgreSQL
•	ORM: Prisma
•	widok tabel AG-grid
•	Autoryzacja: JWT
•	Pliki: OneDrive / SharePoint/serwer (Graph API)
•	API LLM
•	Deployment: Docker


ETAP 2 — Docker (szkielet środowiska)
2.1 docker-compose.yml
•	backend
•	postgres
•	(opcjonalnie redis)
ETAP 3 — Baza danych (fundament
3.2 Modele MVP
•	users
•	roles (user,manager,board,admin)
•	permissions
•	user_roles
•	projects
•	process_nodes
•	process_node_closure
•	audit_log
•	files (metadane)
ETAP 4 — Autoryzacja i RBAC (bez tego ani rusz)
4.1 Auth
•	login
•	refresh token
•	guard JWT
4.2 RBAC
•	enum permissions (TREE_VIEW, TREE_EDIT, FILE_READ, FILE_UPLOAD)
•	guard @RequirePermission()
ETAP 5 — Request Context + Audyt (od razu, nie „kiedyś”)
5.1 Middleware (CLS)
•	request_id
•	user_id
•	ip, user-agent
5.2 Interceptor audytu (diff)
•	@Audit() decorator
•	zapis diffów zmian
•	osobny typ akcji: CREATE / UPDATE / MOVE / DELETE
📌 Efekt: każda zmiana ma ślad (kto, co, kiedy).

